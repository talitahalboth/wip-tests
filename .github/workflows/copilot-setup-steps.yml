name: "Copilot Setup Steps"

# Automatically run the setup steps when they are changed to allow for easy validation, and
# allow manual testing through the repository's "Actions" tab
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  # The job MUST be called `copilot-setup-steps` or it will not be picked up by Copilot.
  copilot-setup-steps:
    name: Setup Copilot
    permissions:
      contents: read
      packages: read
    runs-on: ubuntu-latest

    container:
      image: ghcr.io/actions/actions-runner:latest
      options: >-
        -v /home/runner/work:/home/runner/work
        --dns 8.8.8.8
        --dns 1.1.1.1
      
    steps:
    - name: Verify container environment
      run: |
        echo "=== Container Environment ==="
        echo "Operating System: $(cat /etc/os-release | grep PRETTY_NAME | cut -d'"' -f2)"
        echo "Container working directory: $(pwd)"
        echo "Environment variables:"
        echo "  MOUNTED_TARGET_DIR: $MOUNTED_TARGET_DIR"
        echo "  MOUNTED_TESTS_DIR: $MOUNTED_TESTS_DIR"
        echo "=== End Container Info ==="
    

    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set MOUNTED_SOURCE_DIR
      run: echo "MOUNTED_SOURCE_DIR=$(pwd)" >> $GITHUB_ENV
    

    - name: Test network connectivity
      run: |
        echo "=== Network Connectivity Test ==="
        echo "Testing basic connectivity..."
        timeout 5 bash -c 'exec 3<>/dev/tcp/8.8.8.8/53' 2>/dev/null && echo "✓ DNS connectivity works" || echo "✗ DNS connectivity failed"
        timeout 5 bash -c 'exec 3<>/dev/tcp/google.com/80' 2>/dev/null && echo "✓ HTTP connectivity works" || echo "✗ HTTP connectivity failed"
        echo "Network diagnostic tools available:"
        ./fix_firewall_issues.sh || echo "Network diagnostics completed with warnings"
    
    - name: Run basic validation
      run: |
        echo "=== Basic Validation ==="
        echo "Repository validation completed successfully"
        echo "Copilot setup steps executed"
        echo $MOUNTED_SOURCE_DIR
      